export enum Gender {
  Male = 0,
  Female = 1,
  Other = 2,
}

export enum PartnerType {
  Current = 0,
  Ex = 1,
  Unknown = 2,
}

export enum ConnectionLineType {
  Bezier = "default",
  Straight = "straight",
  Step = "step",
  SmoothStep = "smoothstep",
  SimpleBezier = "simplebezier",
}
